taipy==1.0.0  # Replace with the actual version if available
tensorflow==2.7.0  # Replace with the actual version if available
keras==2.7.0  # Replace with the actual version if available
tensorflow-hub==0.12.0  # Replace with the actual version if available
numpy==1.21.4  # Replace with the actual version if available
pandas==1.3.3  # Replace with the actual version if available